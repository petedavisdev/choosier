{
	"private": true,
	"scripts": {
		"clean": "nuxt cleanup",
		"build": "nuxt build",
		"start": "nuxt start",
		"dev": "nuxt dev",
		"generate": "nuxt generate",
		"prepare": "nuxt prepare",
		"preview": "nuxt preview",
		"checks": "prettier --write . && npm run lint -- --fix && npm run typecheck && npm run e2e",
		"format": "prettier --check .",
		"lint": "eslint .",
		"typecheck": "nuxt typecheck",
		"e2e": "cypress run",
		"cy": "cypress open",
		"supabase:login": "npx supabase login",
		"supabase:types": "npx supabase gen types typescript --project-id 'fbyhdhmmctwoorirepai' --schema public > utils/database.types.ts",
		"upgrade": "npx nuxi@latest upgrade --force"
	},
	"devDependencies": {
		"@nuxt/eslint": "^0.7.4",
		"@nuxt/image": "^1",
		"@nuxtjs/supabase": "^1",
		"base64-arraybuffer": "^1",
		"cypress": "^13",
		"eslint": "^9",
		"eslint-config-prettier": "^9",
		"html-to-image": "^1",
		"nuxt": "^3.15.0",
		"prettier": "^3"
	}
}
