{
	"private": true,
	"scripts": {
		"clean": "nuxt cleanup",
		"build": "nuxt build",
		"start": "nuxt start",
		"dev": "nuxt dev",
		"dev:https": "NODE_TLS_REJECT_UNAUTHORIZED=0 nuxt dev --https --ssl-cert localhost.pem --ssl-key localhost-key.pem",
		"generate": "nuxt generate",
		"prepare": "nuxt prepare",
		"preview": "nuxt preview",
		"checks": "npm run supabase:types && npm run typecheck && npm run lint -- --fix && prettier --write . && npm run e2e",
		"format": "prettier --check .",
		"lint": "eslint .",
		"typecheck": "nuxt typecheck",
		"e2e": "cypress run",
		"cy": "cypress open",
		"supabase:login": "npx supabase login",
		"supabase:types": "npx supabase gen types typescript --project-id 'fbyhdhmmctwoorirepai' --schema public > shared/types/database.ts",
		"upgrade": "npx nuxi@latest upgrade --force",
		"ngrok": "ngrok http http://localhost:3000"
	},
	"dependencies": {
		"@nuxt/image": "^1",
		"@nuxtjs/supabase": "^1",
		"@polar-sh/checkout": "^0.1.11",
		"@polar-sh/nuxt": "^0.3.1",
		"@polar-sh/sdk": "^0.34.2",
		"base64-arraybuffer": "^1",
		"html-to-image": "^1",
		"nuxt": "^4.0.0-alpha.2"
	},
	"devDependencies": {
		"@nuxt/eslint": "^1",
		"cypress": "^14",
		"eslint": "^9",
		"eslint-config-prettier": "^10",
		"prettier": "^3"
	}
}
